@page "/ip"
@using AME.CommonUtils
@attribute [TabItemOption(Text = "IP")]


<Badge Color="Color.Success">
    IP: @WebClientInfo.ClientIpAddress;
</Badge>

@code{
#nullable disable
    [Inject] protected WebClientInfoProvider WebClientInfo { get; set; }
}